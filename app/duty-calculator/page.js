'use client';
import {useState} from 'react';
import {Shell} from '../components';
export default function Duty(){const [v,setV]=useState({value:'10000',rate:'3.7',origin:'China',extra:'10'});const customs=Number(v.value)||0;const duty=customs*(Number(v.rate)||0)/100;const mpf=Math.max(31.67,Math.min(614.35,customs*0.003464));const extra=customs*(Number(v.extra)||0)/100;return <Shell>{()=> <main><section className="page-hero"><h1>AI Duty Calculator</h1><p>Estimate duty, MPF and additional tariff for planning only.</p></section><section className="section feature"><div className="card"><h3>Shipment Inputs</h3><div className="form"><input className="input" value={v.value} onChange={e=>setV({...v,value:e.target.value})} placeholder="Customs Value USD"/><input className="input" value={v.rate} onChange={e=>setV({...v,rate:e.target.value})} placeholder="General Duty Rate %"/><input className="input" value={v.origin} onChange={e=>setV({...v,origin:e.target.value})} placeholder="Country of Origin"/><input className="input" value={v.extra} onChange={e=>setV({...v,extra:e.target.value})} placeholder="Additional Tariff %"/></div></div><div className="card"><h3>Estimated Result</h3><div className="result">Country of Origin: {v.origin}\nGeneral Duty: ${duty.toFixed(2)}\nMPF Estimate: ${mpf.toFixed(2)}\nAdditional Tariff: ${extra.toFixed(2)}\nEstimated Total: ${(duty+mpf+extra).toFixed(2)}\n\nDisclaimer: Planning estimate only. Final HTS, AD/CVD, PGA and tariff review must be confirmed before entry.</div></div></section></main>}</Shell>}
